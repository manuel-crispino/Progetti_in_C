
#include <stdio.h>

char *ft_strstr(char *str, char *to_find)
{
	int i;
	int len;
	
	len= 0; 
	i=0;
	while(to_find[len] != '\0')
		len++; 
	while(str[i] != '\0')
	{
				
	}	
}

int main (void )
{
	return (0);
}

