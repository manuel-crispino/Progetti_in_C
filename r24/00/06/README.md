# Patch files 

<p> 
This is a project that use patch to patch files 
as you can see from the inputs in patch.sh.
first we diff -u a.txt and b.txt to create a newText.patch.
and than we can run the patch  command using patch a.txt < newText.patch
</p>
